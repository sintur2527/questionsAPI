user nginx;

events {
  worker_connections 30000;
}

worker_rlimit_nofile 90000;

http {
  server {
    listen 4000;
      location / {
        proxy_pass http://server:5000;
      }
      location ~ \.(txt) {
        root /server/dist;
      }
  }
}